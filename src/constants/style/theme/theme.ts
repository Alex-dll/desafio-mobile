import { DefaultTheme } from 'styled-components';

import { typographyCommon, spacing, colors } from './common';

export const theme: DefaultTheme = {
  colors,
  typography: typographyCommon,
  spacing,
};
